load("@rules_cc//cc:defs.bzl", "cc_test")

cc_test(
    name = "version_test",
    timeout = "short",
    srcs = ["versiontest.cpp"],
    deps = [
        "//:sl3",
        "//tests:doctest_main",
    ],
)
