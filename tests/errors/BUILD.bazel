load("@rules_cc//cc:defs.bzl", "cc_test")

cc_test(
    name = "error_test",
    timeout = "short",
    srcs = ["errortest.cpp"],
    deps = [
        "//:sl3",
        "//tests:doctest_main",
    ],
)
