load("@rules_cc//cc:defs.bzl", "cc_test")

cc_test(
    name = "commands_test",
    timeout = "short",
    srcs = ["commandstest.cpp"],
    deps = [
        "//:sl3",
        "//tests:doctest_main",
    ],
)
